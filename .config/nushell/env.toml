# No pre-processing
[exact]
RUST_BACKTRACE = "full"
VIRTUAL_ENV_DISABLE_PROMPT = 1

# Prepend path to `.dotfiles` directory
[relative]
XDG_CONFIG_HOME = ".config"
CARGO_HOME = ".cargo"
RUSTUP_HOME = ".rustup"
VIMINIT = ".vimrc"
